```
# pip install virtualenv
cd <path-to-Letter_Detection>
virtualenv myenv (python version == 3.7)
source myenv/bin/activate
pip install -r requirement.txt

## Running experiments

cd Letter_Detection
python src/main.py

